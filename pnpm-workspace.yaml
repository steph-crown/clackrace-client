# pnpm 11+ settings (Corepack may resolve to pnpm 11).
# sharp / unrs-resolver: no postinstall needed for local Next dev.
# esbuild: required by tooling (tsx/vitest/etc. when present).
allowBuilds:
  esbuild: true
  sharp: false
  unrs-resolver: false
